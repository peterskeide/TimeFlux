<p class="notice"><%= flash[:notice] %></p>
<p class="error"><%= flash[:error] %></p>

<h1>Public Holidays</h1>
<%= render :partial => 'sub_menu' -%>

<table>
  <tr>
    <th>Date</th>
    <th>Working Hours</th>
    <th>Note</th>
  </tr>

<% @holidays.each do |holiday| %>
  <tr>
    <td><%=h holiday.show_date %></td>
    <td><%=h holiday.working_hours %></td>
    <td><%=h holiday.note %></td>
    <% if @current_user.admin -%>
      <td><%= link_to image_tag("pencil.png", :title => "Edit"), :action => :edit, :id => holiday %>
          <%= link_to image_tag("delete.png", :title => "Delete"), :action => :destroy, :confirm => 'Really delete holiday?', :method => :delete, :id => holiday %></td>
    <% end %>
  </tr>
<% end %>
</table>

<% if @current_user.admin -%>
<br />
<%= link_to 'New holiday', :action => :create %>
<% end %>